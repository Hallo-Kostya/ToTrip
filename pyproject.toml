[tool.poetry]
name = "totrip"
version = "1.0.0"
description = "A Trip planner for you"
authors = ["RadikUlitki"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^5.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
